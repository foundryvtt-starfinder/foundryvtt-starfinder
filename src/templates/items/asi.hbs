<form class="{{cssClass}} flexcol" autocomplete="off">

    {{!-- Item Sheet Header --}}
    <header class="sheet-header flexrow">
        <img class="profile" src="{{item.img}}" title="{{item.name}}" data-edit="img" />

        <div class="header-details flexrow">
            <h1 class="charname">
                <input name="name" type="text" value="{{item.name}}" placeholder="Item Name" />
            </h1>

            <div class="item-subtitle">
                <h4 class="item-type">{{itemType}}</h4>
                <span class="item-status">{{itemStatus}}</span>
            </div>

            <ul class="summary">
                <li>
                    <input type="text" name="system.source" value="{{itemData.source}}" placeholder="Source" />
                </li>
            </ul>
        </div>
    </header>

    {{!-- Item Sheet Navigation --}}
    <nav class="sheet-navigation tabs" data-group="primary">
        <a class="item active" data-tab="details">{{ localize "SFRPG.Details" }}</a>
    </nav>

    {{!-- Item Sheet Body --}}
    <section class="sheet-body">

        {{!-- Details Tab --}}
        <div class="tab details" data-group="primary" data-tab="details">

            <div class="bubble">
                <h3 class="bubble-header">{{localize "SFRPG.ItemSheet.AbilityScoreIncrease.Header"}}</h3>

                <div class="bubble-info">
                    <blockquote class="quote">{{localize "SFRPG.ItemSheet.AbilityScoreIncrease.InstructionRules"}}</blockquote>

                    <br />
                    <div class="bubble">
                        <h3 class="bubble-header">{{localize "SFRPG.ItemSheet.AbilityScoreIncrease.Instruction"}}</h3>

                        <div class="form-group stacked bubble-info">
                            {{#each config.abilities as |ability name|}}
                            <label class="checkbox" data-tippy-content="{{lookup (lookup ../config.abilities name) "tooltip"}}">
                                <input type="checkbox" name="system.abilities.{{name}}" {{checked (lookup ../itemData.abilities name)}} /> {{ability}}
                            </label>
                            {{/each}}
                        </div>
                    </div>
                </div>
            </div>


    </section>
</form>
