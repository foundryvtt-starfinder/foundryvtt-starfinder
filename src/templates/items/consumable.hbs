<form class="{{cssClass}} flexcol" autocomplete="off">

    {{!-- Item Sheet Header --}}
    {{> "systems/sfrpg/templates/items/parts/item-header.hbs"}}

    {{!-- Item Sheet Navigation --}}
    <nav class="sheet-navigation tabs" data-group="primary">
        <a class="item active" data-tab="description">{{ localize "SFRPG.Description" }}</a>
        <a class="item" data-tab="details">{{ localize "SFRPG.Details" }}</a>
        <a class="item" data-tab="modifiers">{{ localize "SFRPG.Modifiers" }}</a>
    </nav>

    {{!-- Item Sheet Body --}}
    <section class="sheet-body">

        {{!-- Description Tab --}}
        {{> "systems/sfrpg/templates/items/parts/item-description.hbs"}}

        {{!-- Details Tab --}}
        <div class="tab details" data-group="primary" data-tab="details">

            {{!-- Details Tab Navigation --}}
            <nav class="sheet-subnavigation subtabs" data-group="subdetails">
                <a class="item active" data-tab="properties">{{ localize "SFRPG.ItemSheet.Details.Tab.Properties" }}</a>
                <a class="item" data-tab="usage">{{ localize "SFRPG.ItemSheet.Details.Tab.UsageAction" }}</a>
            </nav>

            {{!-- Details Section --}}
            <section class="sheet-details">

                {{!-- Properties Tab --}}
                <div class="tab properties" data-group="subdetails" data-tab="properties">

                    {{!-- Consumable Details --}}
                    <div class="bubble">
                        <h3 class="bubble-header">{{localize "SFRPG.ItemSheet.Details.Section.ConsumableDetails"}}</h3>

                        <div class="bubble-info">

                            {{!-- Consumable Type --}}
                            <div class="form-group">
                                <label>{{ localize "SFRPG.Items.Consumable.Type" }}</label>
                                <select name="system.consumableType">
                                    {{#select itemData.consumableType}}
                                    {{#each config.consumableTypes as |name type|}}
                                    <option value="{{type}}">{{name}}</option>
                                    {{/each}}
                                    {{/select}}
                                </select>
                            </div>

                        </div>
                    </div>

                    {{!-- Physical Properties --}}
                    {{> "systems/sfrpg/templates/items/parts/physical-item-details.hbs"}}
                    
                </div>

                {{!-- Usage & Action Tab --}}
                <div class="tab usage" data-group="subdetails" data-tab="usage">

                    {{!-- Item Activation Template --}}
                    {{#if itemData.activation.type}}
                        {{> "systems/sfrpg/templates/items/parts/item-activation.hbs" message="SFRPG.Items.Consumable.Usage" consumable=true}}
                    {{else}}
                        {{> "systems/sfrpg/templates/items/parts/item-activation.hbs" message="SFRPG.Items.Consumable.Usage" consumable=false}}
                    {{/if}}

                    {{!-- Item Action Template --}}
                    {{> "systems/sfrpg/templates/items/parts/item-action.hbs" message="SFRPG.Items.Consumable.Action"}}

                </div>

            </section>

        </div>

        {{!-- Modifiers Tab --}}
        {{> "systems/sfrpg/templates/items/parts/item-modifiers.hbs"}}
        
    </section>
</form>
