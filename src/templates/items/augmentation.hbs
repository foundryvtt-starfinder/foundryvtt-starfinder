<form class="{{cssClass}} flexcol" autocomplete="off">

    {{!-- Item Sheet Header --}}
    {{> "systems/sfrpg/templates/items/parts/item-header.hbs"}}

    {{!-- Item Sheet Navigation --}}
    <nav class="sheet-navigation tabs" data-group="primary">
        <a class="item active" data-tab="description">{{ localize "SFRPG.Description" }}</a>
        <a class="item" data-tab="details">{{ localize "SFRPG.Details" }}</a>
        <a class="item" data-tab="modifiers">{{ localize "SFRPG.Modifiers" }}</a>
    </nav>

    {{!-- Item Sheet Body --}}
    <section class="sheet-body">
        {{> "systems/sfrpg/templates/items/parts/item-description.hbs"}}

        {{!-- Details Tab --}}
        <div class="tab details" data-group="primary" data-tab="details">
            
            {{!-- Details Tab Navigation --}}
            <nav class="sheet-subnavigation subtabs" data-group="subdetails">
                <a class="item highlight active" data-tab="properties">{{ localize "SFRPG.ItemSheet.Tabs.Properties" }}</a>
                <a class="item" data-tab="attack">{{ localize "SFRPG.ItemSheet.Tabs.AttackDamage" }}</a>
                <a class="item" data-tab="container">{{ localize "SFRPG.ItemSheet.Tabs.AmmunitionContainer" }}</a>
            </nav>

            {{!-- Details Section --}}
            <section class="sheet-details">

                {{!-- Properties Tab --}}
                <div class="tab properties" data-group="subdetails" data-tab="properties">

                    {{!-- Augmentation Details --}}
                    <div class="bubble">
                        <h3 class="form-header bubble-header">{{ localize "SFRPG.Items.Augmentation.Details" }}</h3>

                        <div class="bubble-info">
                            <div class="form-group">
                                <label>{{ localize "SFRPG.Items.Augmentation.Type" }}</label>
                                <div class="form-fields">
                                    <select name="system.type">
                                        {{#select itemData.type}}
                                        {{#each config.augmentationTypes as |augmentation type|}}
                                        <option value="{{type}}">{{augmentation}}</option>
                                        {{/each}}
                                        {{/select}}
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label>{{ localize "SFRPG.Items.Augmentation.System" }}</label>
                                <div class="form-fields">
                                    <select name="system.system">
                                        {{#select itemData.system}}
                                        {{#each config.augmentationSystems as |system key|}}
                                        <option value="{{key}}">{{system}}</option>
                                        {{/each}}
                                        {{/select}}
                                    </select>
                                </div>
                            </div>

                            {{!-- Equipment Status --}}
                            {{> "systems/sfrpg/templates/items/parts/item-status.hbs"}}

                        </div>
                    </div>
                    
                    {{!-- Weapon Usage --}}
                    <div class="bubble">
                        <h3 class="form-header bubble-header">{{ localize "SFRPG.Items.Weapon.Usage" }}</h3>
                            
                        <div class="bubble-info">
                            {{!-- Item Activation Template --}}
                            {{> "systems/sfrpg/templates/items/parts/item-activation.hbs"}}
                        </div>
                    </div>

                    {{!-- Physical Properties --}}
                    <div class="bubble">
                        <h3 class="form-header bubble-header">{{ localize "SFRPG.ItemSheet.PhysicalProperties.Header" }}</h3>

                        <div class="bubble-info">
                            {{> "systems/sfrpg/templates/items/parts/physical-item-details.hbs"}}
                        </div>
                    </div>

                </div>

                {{!-- Attack & Damage Tab --}}
                <div class="tab attack" data-group="subdetails" data-tab="attack">

                    {{!-- Item Action Template --}}
                    {{> "systems/sfrpg/templates/items/parts/item-action.hbs"}}

                </div>

                {{!-- Container Tab --}}
                <div class="tab container" data-group="subdetails" data-tab="container">

                    {{!-- Container Template --}}
                    {{> "systems/sfrpg/templates/items/parts/container-details.hbs" message="SFRPG.Items.Equipment.ContainerTooltip"}}

                </div>

            </section>
        </div>

        {{!-- Modifiers Tab --}}
        {{> "systems/sfrpg/templates/items/parts/item-modifiers.hbs"}}
    </section>
</form>
